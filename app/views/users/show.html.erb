<article class="hentry">
	<header>
		<h1><%= @user.name %>'s Articles</h1>
		
		<% if current_user %>
			<ul class="inll article-actions">
				<li class="edit"><%= link_to 'Edit', edit_user_path(@user) %></li>
				<li class="delete"><%= link_to 'Delete', @user, :confirm => "Are you sure you want to delete this article?", :method => :delete %>
			</ul>
		<% end %>
	</header>
	
	<div class="entry-content hfeed">
		<% @user.articles.reverse_each do |article| %>
			<article class="hentry">
				<h2 class="entry-title"><%= link_to article.title, article, :rel => 'bookmakr' %></h2>
				<div class="entry-summary">
					<%= raw truncate(article.body, :length => 300) %>
				</div>
			</article>
		<% end %>
	</div>
</article>