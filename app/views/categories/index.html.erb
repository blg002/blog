<% content_for :title, "Categories" %>

<h1>Categories</h1>

<ul>
	<% @categories.each do |category| %>
		<li><%= link_to category.name, category %> (<%= category.articles.length %>)</li>
	<% end %>
</ul>